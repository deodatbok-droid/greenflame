name: Deploy to production

on:
  push:
    branches: [master]

jobs:
  sync-to-main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Push master → main (Vercel production branch)
        run: git push origin HEAD:main
